# https://hub.docker.com/r/otel/opentelemetry-collector-contrib/tags#!
version: "3"

services:
  sourcecode-editor:
    image: devopsplaybookio/sourcecode-editor
    ports:
      - 8080:8080
    volumes:
      - ./data:/data
